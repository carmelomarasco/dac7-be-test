package it.finanze.entrate.coopint.dboard.dpi.com.command.utility;

import lombok.extern.apachecommons.CommonsLog;

@CommonsLog
public class XmlOperation {
/*
    public static DPIOECD getXmlObject(String xmlGzipBase64) throws Exception {   
    	// TODO
        return GestioneControlli.unmarshallAndGetDPIOECD(IOUtils.gUnzipper(Base64.getDecoder().decode(xmlGzipBase64)));
    }
    public static byte[] getXmlBytes(String xmlGzipBase64) throws IOException {
        log.info("Convert xml string to bytes");
        byte[] xmlGzip = Base64.getDecoder().decode(xmlGzipBase64);
        byte[] xmlFile = IOUtils.gUnzipper(xmlGzip);
        return xmlFile;
    }
    */
}
